$(document).ready(function() {
  var detectParentSize = function(node) {
    // Grab the parent element
    // find it's height
    //
  };
  console.log('This runs');

  var createNewChildrenContainer = function(size) {
    // creates four containing elements for the 
    // four children that will come from the selected circle
  };
})